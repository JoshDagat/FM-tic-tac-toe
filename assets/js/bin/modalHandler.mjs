function showModal(identifier) {
  const modal = document.querySelector(`${identifier}`)
        modal.classList.add('active');
}

function hideModal(identifier) {
  const modal = document.querySelector(`${identifier}`)
}

function saveSettings() {
  hideModal('#modal-settings')

  const audioSave = document.querySelector('#')
}

modal-base-result
modal-tied-result
modal-audio-toggle
modal-settings
modal-reset
modal-restore-session